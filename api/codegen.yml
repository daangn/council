schema: ./src/public/sdl.graphql

documents:
  - ./src/client/pages/**/*.tsx

generates:
  ./src/client/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
    config:
      skipTypename: true
      avoidOptionals: true
