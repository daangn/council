import * as React from 'react';

import { type PageContext } from '~/client/ssr';

export async function getPageProps({ app }: PageContext) {
}

export default function SignupRequestedPage() {
  return (
    <div>

    </div>
  );
}
